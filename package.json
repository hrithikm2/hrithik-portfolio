{
  "name": "hrithik-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name hrithik-portfolio"
  }
}
